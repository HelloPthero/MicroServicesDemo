```
到consul.exe在的文件夹 cmd运行
consul agent -dev
```

```
启动服务
单个：
dotnet run --urls http://localhost:1001

集群：
通过不同json配置环境 根据环境调不同端口 
appsetting.test1.json 
appsetting.test2.json 
dotnet run --environment test1 --urls http://localhost:1001
dotnet run --environment test2 --urls http://localhost:1002
```

